{{rules}}

{{skill}}

## Output schema (items.schema.json)

```json
{{schema}}
```

{{familySkill}}

{{instincts}}

Composition family: {{compositionFamily}}

Below is a window of paragraphs. Type the ones marked with a character count; the others are
context only and must not appear in your answer.

Offsets are 0-based character positions into the paragraph's own text, exactly as shown.

{{window}}

Return JSON matching the schema above: for each paragraph you commit, the offsets its items start
at, and the references you can see. Do not return any text of the paragraph itself.
