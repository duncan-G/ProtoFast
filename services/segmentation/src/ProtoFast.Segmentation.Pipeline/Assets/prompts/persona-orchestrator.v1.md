{{rules}}

{{skill}}

## Output schema (registry-plan.schema.json)

```json
{{schema}}
```

{{familySkill}}

Document: {{documentTitle}}

The window agents have reported. Build the registry.

{{digest}}

Return JSON matching the schema above: a `registry` when you can assign every candidate, or
`followUps` when an answer would change what you write. `gaps` may be returned either way, and
`[]` is the right answer when there is nothing to record.
