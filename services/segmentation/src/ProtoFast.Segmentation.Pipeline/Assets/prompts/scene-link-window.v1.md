{{rules}}

{{skill}}

## Output schema (scene-links.schema.json)

```json
{{schema}}
```

{{familySkill}}

Below is a run of consecutive scene digests.

{{window}}

Return JSON matching the schema above. An empty `links` array is a correct and common answer. Put
anything whose other endpoint is outside this run into `openQuestions`.
