{{rules}}

{{skill}}

## Output schema (scene-cut.schema.json)

```json
{{schema}}
```

{{familySkill}}

{{instincts}}

Composition family: {{compositionFamily}}

Below is one section's items, in order, with the mode and speaker code derived for each, and the
boundaries code proposed with the reason for each.

{{section}}

Return JSON matching the schema above: a verdict for each proposed boundary, and for each scene
your verdicts produce, its setting, subject, time and title. Leave `placeId` empty for a scene the
text does not locate.
