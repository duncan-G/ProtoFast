{{rules}}

{{skill}}

## Output schema ({{schemaName}})

```json
{{schema}}
```

Item: {{itemId}} ({{itemKind}})

The span, exactly as the document has it:

```
{{span}}
```

People this span's own references resolve to: {{personas}}

Return JSON matching the schema above. Use only the words of the span, the names listed above, and
function words — anything else is an invention and the answer is discarded.
