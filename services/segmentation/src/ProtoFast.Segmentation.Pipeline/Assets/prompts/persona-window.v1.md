{{rules}}

{{skill}}

## Output schema (persona-window.schema.json)

```json
{{schema}}
```

{{familySkill}}

{{instincts}}

Below is a window of paragraphs with every reference in them that code could not resolve on its
own. Cluster the ones that refer to the same thing.

{{window}}

Return JSON matching the schema above. Use only the tag ids shown, each at most once, and put
anything you cannot settle from inside this window into `openQuestions`.
