<#ftl output_format="plainText">
${msg("pfVerifyHeading")}

${msg("pfVerifyLead", realmName)}

${link}

${msg("pfLinkExpires", linkExpirationFormatter(linkExpiration))}

${msg("pfVerifyIgnore", realmName)}
