# Angular client + proto codegen — moved

This content has moved to the composable `add-angular-client` skill:

- **Angular setup (Steps 4a–4f):** `.cursor/add-angular-client/references/angular-setup.md`
- **Proto codegen (Steps 5a–5f):** `.cursor/add-angular-client/references/proto-codegen.md`

The `bootstrap-project` skill now delegates to `add-angular-client`
in Step 4–5 instead of loading this file directly.
